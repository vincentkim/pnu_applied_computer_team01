<!DOCTYPE html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/adminResetPW.css" />
    <title>Reset Password</title>
</head>
<body class="align">
    <script type="text/javascript">
        var err="<%=err%>";
        if(err!=""){
            alert(err);
        }
    </script>
    <%if(validity=='valid'){%>
    <div class="site__container">

        <div class="grid__container">
            
          <form action="/admin/resetPW/Checked" method="post" class="form form--resetPW">
            <div class="form__header">
                <h1>Reset Password</h1>
                    <div class="form__subHeader">
                        <h2>-Check Verification Code-</h2>
                        <h3>Check verificatioin code in your email!</h3>
                    </div>
            </div>
              <div class="form__field">
                <label class="fontawesome-lock" for="resetPW__password"><span class="hidden">Password</span></label>
                <input name="password" id="resetPW__password" type="password" class="form__input" placeholder="Password" required>
              </div>
              <div class="form__field">
                  <label class="fontawesome-ok-sign" for="resetPW__passwordCheck"><span class="hidden">PasswordCheck</span></label>
                  <input name="passwordCheck" id="resetPW__passwordCheck" type="password" class="form__input" placeholder="Password check" required>
              </div>
              <div class="form__field">
                <label class="fontawesome-ok-sign" for="resetPW__verificationCode"><span class="hidden">VerificationCode</span></label>
                <input name="verificationCode" id="resetPW__verificationCode" type="text" class="form__input" placeholder="Insert verification code" required>
              </div>
            <div class="form__field">
              <input type="submit" value="Submit">
            </div>
            <input name="email" style="display:none;" value="<%=email%>">
            <input name="token" style="display:none;" value="<%=token%>">
          </form>
        </div>
        <%}else{%>
                <h1 style="color:white;margin-left: 800px;">Invalid Access</h1>\
        <%}%>
      </div>   
</body>